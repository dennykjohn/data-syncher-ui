import { Flex } from "@chakra-ui/react";

const Usage = () => {
  return (
    <Flex flexDirection="column" gap={4}>
      <div>Usage content goes here</div>
    </Flex>
  );
};

export default Usage;
