import { Flex } from "@chakra-ui/react";

const Schema = () => {
  return (
    <Flex flexDirection="column" gap={4}>
      <div>Schema content goes here</div>
    </Flex>
  );
};

export default Schema;
